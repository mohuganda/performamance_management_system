deploy/.env
deploy/docker-compose.override.yml

# Runtime / persistent data (MySQL, Redis, API storage, logs) — never commit
data/

# Go / Air dev build output
backend/tmp/
backend/build-errors.log

# Host-built smoke/test binaries (Colima bind-mount path)
.tmp/
backend/.tmp/
