# Local dev (Vite proxies /api to http://127.0.0.1:3030)
VITE_API_BASE_URL=/api/v1

# Production / direct API access (uncomment if not using a reverse proxy)
# VITE_API_BASE_URL=http://localhost:3030/api/v1
