import { schemaMigrations } from '@nozbe/watermelondb/Schema/migrations';

export default schemaMigrations({
  migrations: [
    // Future migrations will be added here using `addColumns` or `createTable`
  ],
});
